import React from "react";

import {LoginPageContainer, LoginForm } from './styles'

export default function LoginPage(props) {
  return (
    <LoginPageContainer>
      <LoginForm>
        {/* Conteúdo do formulário de login aqui */}
        <h2>Login Form</h2>
        {/* Adicione o formulário de login ou qualquer outro conteúdo aqui */}
      </LoginForm>
    </LoginPageContainer>
  );
}